spring:
  jpa:
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: ${JWT_SECRET:Y2xpbmljU2Fhc0p3dFNlY3JldEtleUZvckRldmVsb3BtZW50T25seVVzZQ==}
  encryption:
    key: ${PII_ENCRYPTION_KEY:dGVzdEVuY3J5cHRpb25LZXlGb3JEZXZlbG9wbWVudCE=}
  platform-db:
    url: ${PLATFORM_DB_URL:jdbc:postgresql://localhost:5432/clinic_platform}
    username: ${PLATFORM_DB_USER:clinic_admin}
    password: ${PLATFORM_DB_PASSWORD:clinic_secret}
  tenant-db:
    admin-user: ${POSTGRES_ADMIN_USER:clinic_admin}
    admin-password: ${POSTGRES_ADMIN_PASSWORD:clinic_secret}

logging:
  level:
    com.clinicsaas: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG

springdoc:
  swagger-ui:
    enabled: true
