import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primary = Color(0xFF2563EB);
  static const primaryDark = Color(0xFF1D4ED8);
  static const primaryLight = Color(0xFFEFF6FF);
  static const accent = Color(0xFF6366F1);

  // Backgrounds
  static const background = Color(0xFFF8FAFC);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF1F5F9);
  static const sidebar = Color(0xFF0F172A);
  static const sidebarSurface = Color(0xFF1E293B);

  // Text
  static const textPrimary = Color(0xFF0F172A);
  static const textSecondary = Color(0xFF64748B);
  static const textTertiary = Color(0xFF94A3B8);
  static const textOnDark = Color(0xFFE2E8F0);

  // Border
  static const border = Color(0xFFE2E8F0);
  static const borderLight = Color(0xFFF1F5F9);

  // Status
  static const statusNew = Color(0xFF2563EB);
  static const statusOpen = Color(0xFF4F46E5);
  static const statusPending = Color(0xFFD97706);
  static const statusOnHold = Color(0xFF64748B);
  static const statusResolved = Color(0xFF16A34A);
  static const statusClosed = Color(0xFF475569);

  // Status backgrounds
  static const statusNewBg = Color(0xFFEFF6FF);
  static const statusOpenBg = Color(0xFFEEF2FF);
  static const statusPendingBg = Color(0xFFFFFBEB);
  static const statusOnHoldBg = Color(0xFFF8FAFC);
  static const statusResolvedBg = Color(0xFFF0FDF4);
  static const statusClosedBg = Color(0xFFF8FAFC);

  // Priority
  static const priorityCritical = Color(0xFFDC2626);
  static const priorityHigh = Color(0xFFEA580C);
  static const priorityMedium = Color(0xFF2563EB);
  static const priorityLow = Color(0xFF64748B);

  // Priority backgrounds
  static const priorityCriticalBg = Color(0xFFFEF2F2);
  static const priorityHighBg = Color(0xFFFFF7ED);
  static const priorityMediumBg = Color(0xFFEFF6FF);
  static const priorityLowBg = Color(0xFFF8FAFC);

  // Semantic
  static const success = Color(0xFF16A34A);
  static const successBg = Color(0xFFF0FDF4);
  static const error = Color(0xFFDC2626);
  static const errorBg = Color(0xFFFEF2F2);
  static const warning = Color(0xFFD97706);
  static const warningBg = Color(0xFFFFFBEB);
  static const info = Color(0xFF2563EB);
  static const infoBg = Color(0xFFEFF6FF);

  // Gradient
  static const gradientStart = Color(0xFF2563EB);
  static const gradientEnd = Color(0xFF4F46E5);

  // ─── Dark Mode Palette ───────────────────────────────────────────────────────
  static const backgroundDark = Color(0xFF0F172A);
  static const surfaceDark = Color(0xFF1E293B);
  static const borderDark = Color(0xFF334155);
  static const textPrimaryDark = Color(0xFFF1F5F9);
  static const textSecondaryDark = Color(0xFF94A3B8);
  static const textTertiaryDark = Color(0xFF64748B);
  static const surfaceVariantDark = Color(0xFF1E293B);
  static const sidebarDark = Color(0xFF020617);
}
